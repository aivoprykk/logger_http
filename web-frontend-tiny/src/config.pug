extends layout.pug

block content
  main
    .container
      article.card.config
        header.card-header
          .flexrow
            h2 Logger configuration
        .card-body
          img(src="assets/logo.svg")